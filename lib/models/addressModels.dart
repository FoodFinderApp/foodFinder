class AddressLoc {
  String? currentAddress;
  String? homeTitle;
  String? placeDetails;
  String? phNumber;
  String? orderId;
  double? latitude;
  double? longitude;

  AddressLoc(
      {this.currentAddress,
      this.homeTitle,
      this.placeDetails,
      this.phNumber,
      this.orderId,
      this.latitude,
      this.longitude});
}
